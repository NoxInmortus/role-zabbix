---
- include: validate.yml

- include: install.yml
  tags:
    - zabbix_agent
    - zabbix_install

- include: config.yml
  tags:
    - zabbix_agent
    - zabbix_config

- include: agent/config.yml
  tags:
    - zabbix_agent
    - zabbix_config

# - include: external.yml
#   tags:
#     - zabbix_agent
#     - zabbix_config
#     - zabbix_external
